# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SwiftUITryout' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SwiftUITryout

  target 'SwiftUITryoutTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SwiftUITryoutUITests' do
    # Pods for testing
  end

end
